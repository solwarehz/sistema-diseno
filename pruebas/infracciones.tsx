// Archivo de prueba: cada linea infringe una regla del candado a proposito.
export function Malo() {
  const hex = '#0063CB';
  const funcion = 'rgba(0, 99, 203, .5)';
  const arbitrario = 'bg-[#fff]';
  const tamano = 'text-[13px]';
  const contorno = 'outline-none';
  const primitiva = 'bg-primitiva-azul-600';
  const campana = 'text-marca-amarillo';
  const peso = 'font-extrabold';
  return { hex, funcion, arbitrario, tamano, contorno, primitiva, campana, peso };
}
